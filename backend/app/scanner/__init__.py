"""Opportunity Scanner (M13a).

Ranks the ingested universe by a transparent, deterministic composite built
from the existing valuation scorecards. This is a *screen*, not advice: it
surfaces research candidates with confidence + missing-data flags and links to
their evidence. It never emits buy/sell calls.
"""
